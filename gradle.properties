org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

# arm64 only (ADR-0013, ADR-0011). The emulator has neither a real camera nor a real display
# pipeline, so an x86_64 slice would build code we cannot meaningfully run.
